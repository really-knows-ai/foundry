name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-all:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - node-label: LTS 22
            node-version: '22.x'
          - node-label: LTS 24
            node-version: '24.x'
          - node-label: current 25
            node-version: '25.x'
          - node-label: current 26
            node-version: '26.x'

    steps:
      - uses: actions/checkout@v6

      - name: Enable corepack
        run: corepack enable

      - name: Use Node.js ${{ matrix.node-label }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Configure git
        run: |
          git config --global user.name "CI Runner"
          git config --global user.email "ci@example.com"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint, test, and build
        run: pnpm run build:all
